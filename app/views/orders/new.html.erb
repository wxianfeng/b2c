<%= title "#{t(:creating_order)}" %>

<% semantic_form_for @order, :url => orders_url do |f| %>
 <% f.inputs do  %>
	<%= render  :partial => 'form', :locals => {:f => f} %>
 <% end %>
 <% f.buttons do %>
   <%= f.commit_button t(:create) %>
   <%= f.cancel_link    %> 
 <% end %>
<% end %>

