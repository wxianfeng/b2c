<%= title "#{t(:editing_offer)}" %>

<% semantic_form_for @offer, :url => offer_url do |f| %>
 <% f.inputs do  %>
	<%= render  :partial => 'form', :locals => {:f => f} %>
 <% end %>
 <% f.buttons do %>
   <%= f.commit_button t(:save) %>
   <%= f.cancel_link    %> 
 <% end %>
<% end %>

