<%= title "#{t(:editing_item)}" %>

<% semantic_form_for @item, :url => item_url do |f| %>
 <% f.inputs do  %>
	<%= render  :partial => 'form', :locals => {:f => f} %>
 <% end %>
 <% f.buttons do %>
   <%= f.commit_button t(:save) %>
   <%= f.cancel_link    %> 
 <% end %>
<% end %>

